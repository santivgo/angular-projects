import { FocusSecondDirective } from './focus-second.directive';

describe('FocusSecondDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusSecondDirective();
    expect(directive).toBeTruthy();
  });
});
