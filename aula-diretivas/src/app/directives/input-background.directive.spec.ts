import { InputBackgroundDirective } from './input-background.directive';

describe('InputBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new InputBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
